# Copy to ~/.praam/secrets.env (never commit real keys)
#
# LiteLLM reads these via docker-compose env_file.
# Apps do not need provider keys when using the platform gateway.

OPENAI_API_KEY=sk-your-openai-key-here
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
