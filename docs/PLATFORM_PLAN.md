# praam-platform — Phase 1 implementation plan

**Status:** Approved for implementation (Architecture grade: A)  
**Last updated:** 2026-06-03

## Final decision

**Architecture: A — approved to build.**

Remaining risk is **execution discipline** (ordered schema migration PRs across siblings), not design.

### Three mandatory adjustments (locked before coding)

| # | Change | Replaces |
|---|--------|----------|
| 1 | **Generated env files** — `make render-env` writes `.env.platform.generated` in each app repo; apps load `.env` + generated file; **never mutate** sibling `.env` | `sync-platform-env.sh` |
| 2 | **Named Redis roles** — `redis.cache`, `redis.celery`, etc. in `services.yaml` | `redis_db: [0, 1]` arrays |
| 3 | **Service metadata** — `type`, `health`, `dependencies` per app in `services.yaml` | ports-only entries |

### Reserved now, implement later

| Item | Phase |
|------|-------|
| `sdk/python/`, `sdk/typescript/` (README + namespace only) | Post–Phase 1 |
| `make verify-schema` in platform + hook from `platform-wait` | Phase 1 |
| LiteLLM **model aliases** (`fast`, `reasoning`, `embedding`) in `config/litellm.yaml` | Phase 1 |
| Prometheus / Grafana / Tempo | Phase 3 |
| Ollama + model cache | Phase 2 |
| K8s / Helm | Phase 4 |

---

## Layer model (evolution path)

```text
praam-platform (today → future)
├── postgres + redis + litellm     [Phase 1]
├── services.yaml (platform API)   [Phase 1]
├── make/v1 + render-env + doctor  [Phase 1]
├── verify-schema                  [Phase 1]
├── sdk/python + sdk/typescript    [reserved]
├── secrets (~/.praam/secrets.env) [Phase 1]
├── ollama + model cache           [Phase 2]
├── prometheus + grafana + tempo   [Phase 3]
└── kubernetes / helm              [Phase 4]
```

```text
Application Layer
├── praam-demo-hub
├── praam-knowledge-studio
├── findoc-ai
├── prism (PraamOps)
├── askHR
├── production-plan-copilot
└── praam-pulse

Platform Layer (this repo)
├── PostgreSQL (praam_dev, multi-schema)
├── Redis (named logical DBs)
├── LiteLLM (provider gateway)
├── praam-network
└── services.yaml

Developer Layer
├── make/v1/platform.mk
├── render-env / doctor / verify-schema
└── praam-demo-hub suite orchestration
```

---

## Env contract: generated files (no mutation)

```text
services.yaml  →  render-env.sh  →  <app>/.env.platform.generated
```

**Per app repo:**

```text
findoc-ai/backend/
  .env                      # local overrides, never written by platform
  .env.platform.generated   # gitignored; regenerated by make render-env
```

**Load order (later overrides earlier):**

```python
load_dotenv(".env")
load_dotenv(".env.platform.generated")
```

**Suite / hub:** before sibling `make up`, run `make render-env` in each repo. **Do not** implement `sync-platform-env.sh`.

**Doctor:** compare rendered output to on-disk `.env.platform.generated` (not private `.env`).

---

## `services.yaml` v1 shape (locked)

```yaml
version: 1

platform:
  postgres:
    host_port: 15430
    container_host: postgres
    database: praam_dev
  redis:
    host_port: 16380
    container_host: redis
  litellm:
    host_port: 3100
    container_host: litellm
    path: /v1
    models:
      fast: gpt-4o-mini
      reasoning: gpt-4o
      embedding: text-embedding-3-large
    providers:
      default: openai

apps:
  findoc-ai:
    type: product
    runtime: host
    ui: 3003
    api: 3013
    schema: findoc
    health:
      api: /health
    dependencies:
      - postgres
      - redis
      - litellm
    redis:
      cache: 2

  knowledge-studio:
    type: product
    runtime: docker
    ui: 3001
    api: 3011
    schema: knowledge_studio
    health:
      api: /health
    dependencies:
      - postgres
      - redis
      - litellm
    redis:
      cache: 0
      celery: 1

  prism:
    type: product
    runtime: docker
    ui: 3004
    api: 3014
    schema: prism
    health:
      api: /health
    dependencies:
      - postgres
      - redis
      - litellm
    redis:
      cache: 3
      celery: 4
      results: 5

  askhr:
    type: product
    runtime: docker
    ui: 3005
    api: 3015
    schema: askhr
    health:
      api: /health
    dependencies:
      - postgres
      - redis
      - litellm
    redis:
      cache: 6
      celery: 7

  production-plan-copilot:
    type: product
    runtime: host
    ui: 3006
    api: 3016
    schema: plan_copilot
    health:
      api: /health
    dependencies:
      - postgres
      - litellm

  praam-pulse:
    type: product
    runtime: docker
    api: 3007
    realtime: 3027
    schema: pulse
    health:
      api: /v1/health/live
    dependencies:
      - postgres

  demo-hub:
    type: shell
    runtime: docker
    ui: 3000
    dependencies: []
```

---

## Postgres: one database, many schemas

| Schema | App |
|--------|-----|
| `knowledge_studio` | praam-knowledge-studio |
| `findoc` | findoc-ai |
| `prism` | prism |
| `askhr` | askHR |
| `plan_copilot` | production-plan-copilot |
| `pulse` | praam-pulse |

**Platform init** (`infra/postgres/init/01-schemas.sql`): schemas + grants + `CREATE EXTENSION vector` only — never app tables.

**App migrations:** Alembic inside each schema. See [SCHEMA_MIGRATIONS.md](SCHEMA_MIGRATIONS.md) (created during implementation).

---

## Connectivity

- **Container → platform:** Docker DNS on `praam-network` — `postgres`, `redis`, `litellm`
- **Host-run apps** (findoc, plan-copilot): `127.0.0.1:15430`, `127.0.0.1:16380`, `127.0.0.1:3100`
- **`PRAAM_USE_PLATFORM=0`:** per-repo local Postgres/Redis (escape hatch; must stay in CI)

---

## Versioned platform API

Apps pin:

```makefile
PRAAM_PLATFORM_API ?= v1
-include $(PRAAM_PLATFORM_ROOT)/make/$(PRAAM_PLATFORM_API)/platform.mk
```

**v1 stable targets:** `platform-ensure`, `platform-wait`, `platform-doctor`, `render-env`, `verify-schema`

---

## LiteLLM (Phase 1)

- Secrets only in `~/.praam/secrets.env`
- Apps use model **aliases** (`fast`, `reasoning`, `embedding`) via gateway — not raw provider IDs in app repos

---

## Platform SDK (reserved)

```text
sdk/python/     # future: from praam_platform import get_database_url
sdk/typescript/ # future: getDatabaseUrl(), getLitellmUrl()
```

Phase 1 uses `render-env.sh` only.

---

## Target repo layout

```text
praam-platform/
├── VERSION
├── services.yaml
├── docker-compose.yml
├── config/litellm.yaml
├── infra/postgres/init/01-schemas.sql
├── Makefile
├── make/v1/platform.mk
├── scripts/v1/
│   ├── render-env.sh
│   ├── doctor.sh
│   └── verify-schema.sh
├── sdk/
├── docs/
│   ├── PLATFORM_PLAN.md    # this file
│   └── SCHEMA_MIGRATIONS.md
├── .env.secrets.example
└── README.md
```

---

## Developer contract

```text
make up  →  doctor (optional)  →  platform-ensure  →  platform-wait
         →  render-env  →  verify-schema  →  up-app
```

---

## Implementation order

| Step | Work |
|------|------|
| 1 | Scaffold this repo (compose, services.yaml, make/v1, scripts, sdk stubs) |
| 2 | **findoc-ai** proof (host runtime, render-env, schema migration) |
| 3 | **praam-demo-hub** suite wiring |
| 4 | Remaining siblings (ordered migrations) |
| 5 | CI: doctor, platform mode, standalone mode |

---

## Implementation checklist

- [ ] `platform-repo-v1` — compose, schemas, litellm, services.yaml, make/v1, doctor, verify-schema
- [ ] `render-env-generated` — `.env.platform.generated` + gitignore in apps
- [ ] `hub-delegate` — hub suite; no `sync-platform-env.sh`
- [ ] `findoc-proof` — end-to-end host runtime
- [ ] `sibling-rollout` — compose network + Alembic per SCHEMA_MIGRATIONS.md
- [ ] `verify-suite-ci` — PRAAM_USE_PLATFORM 0/1

---

## Review consensus

- Single DB + multi-schema, versioned `make/v1`, `services.yaml`, shared network, LiteLLM in Phase 1
- Generated env files, named Redis, service metadata, verify-schema, model aliases
- **Removed:** mutating sibling `.env` via sync scripts
- **Main execution risk:** ordered sibling migration PRs
